# Resend — https://resend.com/api-keys
# Variáveis do SERVIDOR (Vercel → Settings → Environment Variables).
# Não uses o prefixo VITE_ — não devem aparecer no browser.

RESEND_API_KEY=re_xxxxxxxx
BOOKING_TO_EMAIL=cristina@exemplo.pt

# Remetente (domínio verificado na Resend ou, para testes, onboarding@resend.dev):
BOOKING_FROM_EMAIL=Chalé Arraiolos <onboarding@resend.dev>
